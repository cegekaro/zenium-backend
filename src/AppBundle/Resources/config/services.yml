services:
    app.abstract.manager:
        class: AppBundle\Manager\AbstractManager
        arguments: [@doctrine.orm.entity_manager]
        abstract: true

    app.question_category.manager:
        class: AppBundle\Manager\QuestionCategoryManager
        arguments: [@doctrine.orm.entity_manager]
        parent: app.abstract.manager
